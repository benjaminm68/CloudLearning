{% extends 'base.html.twig' %}

{% block title %}Liste des utilisateurs
{% endblock %}

{% block body %}
	<h1>Liste des users</h1>

	<ul>
		{% for user in users %}
			<li>
				<a href="{{ path('user_show', {'id':user.id} ) }}">{{ user.nom ~ ' ' ~ user.prenom ~ ' ' ~  user.email }}</a>
			</li>
		{% endfor %}
	</ul>
	{# <div class="flex btn">
		<a href="" class="btn btn-primary">Ajouter</a>
	</div> #}

		{# <a href="{{ path('',) }}" class="btn btn-warning">Modifier</a>
		<a href="{{ path('',) }}" class="btn btn-danger">Supprimer</a> #}

{% endblock %}
