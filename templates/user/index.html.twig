{% extends 'base.html.twig' %}

{% block title %}Liste des utilisateurs
{% endblock %}

{% block body %}
<h1>Liste des users</h1>

	<a class="btn btn-primary" href="{{ path('user_add') }}"> Ajouter un user</a>

<table class="table table-striped table-dark">
  <thead>
    <tr>
      <th scope="col">Nom prénom</th>
      <th scope="col">Role</th>
      <th scope="col">Email</th>
      <th scope="col">Action</th>
    </tr>
  </thead>
  <tbody>
  
{% for user in users %}

<tr>
    <td>{{user.prenom ~' '~ user.nom }}</td>
    <td>ROLE</td>
    <td>{{user.email }}</td>
   <td><a href="{{ path('user_delete', {'id':user.id }) }}"><i class="far fa-trash-alt"></i></a> <a href="{{ path('user_edit', {'id':user.id }) }}"><i class="far fa-edit"></i></a></td>
    
{% endfor %}

  </tbody>
</table>

{% endblock %}
