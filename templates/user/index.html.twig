{% extends 'base.html.twig' %}

{% block title %}Hello UserController!
{% endblock %}

{% block body %}
	<h1>Liste des users</h1>

	<ul>
		{% for user in users %}
			<li>
				<a href="{{ path('user_show', {'id':user.id} ) }}">{{ user.email }}</a>
			</li>
		{% endfor %}
	</ul>

{% endblock %}
